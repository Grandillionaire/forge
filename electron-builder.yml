appId: app.forge.studio
productName: Forge
directories:
  buildResources: build
files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!scripts/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{tsconfig,tsconfig.node,tsconfig.web}.json"
asarUnpack:
  - resources/**

publish:
  provider: github
  releaseType: release

mac:
  category: public.app-category.graphics-design
  target:
    - target: dmg
      arch: [arm64, x64]
  # Ad-hoc codesigning. Without it, macOS Sonoma+ shows a hard "Forge is damaged
  # and cannot be opened" message for any quarantined download — no right-click
  # bypass. Ad-hoc converts that to the older "unidentified developer" prompt
  # which right-click → Open does dismiss. A real Developer ID still costs $99/yr.
  identity: "-"
  hardenedRuntime: false
  gatekeeperAssess: false
dmg:
  artifactName: ${productName}-${version}-${arch}.${ext}

win:
  target:
    - target: nsis
      arch: [x64, arm64]
  artifactName: ${productName}-${version}-${arch}.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true

linux:
  target:
    - target: AppImage
      arch: [x64, arm64]
  category: Graphics
  artifactName: ${productName}-${version}-${arch}.${ext}
